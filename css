/* Reset básico para o layout */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Corpo do site */
body {
  font-family: 'Arial', sans-serif;
  background-color: #f4f7fa;
  color: #333;
  line-height: 1.6;
  padding: 0;
  margin: 0;
}

/* Cabeçalho */
header {
  background-color: #1aab8a; /* Cor verde, comum em farmácias */
  color: white;
  padding: 20px 0;
  text-align: center;
}

header h1 {
  font-size: 2.5rem;
  margin-bottom: 10px;
}

/* Navegação */
nav ul {
  display: flex;
  justify-content: center;
  gap: 20px;
  list-style: none;
}

nav a {
  color: white;
  text-decoration: none;
  font-weight: bold;
  text-transform: uppercase;
}

nav a:hover {
  color: #ffb703; /* Cor dourada de hover */
}

/* Banner principal */
.banner {
  background-image: url('imagem-banner.jpg'); /* Altere para o caminho da imagem */
  background-size: cover;
  background-position: center;
  height: 400px;
  color: white;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
}

.banner h2 {
  font-size: 3rem;
  font-weight: bold;
}

/* Seção de produtos */
.section-products {
  padding: 40px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 30px;
}

.product {
  background-color: white;
  border-radius: 8px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  text-align: center;
}

.product img {
  width: 100%;
  height: auto;
}

.product h3 {
  color: #333;
  padding: 10px;
}

.product p {
  padding: 10px;
  color: #666;
}

.product button {
  background-color: #1aab8a;
  color: white;
  border: none;
  padding: 10px;
  width: 100%;
  cursor: pointer;
  font-weight: bold;
}

.product button:hover {
  background-color: #ffb703;
}

/* Rodapé */
footer {
  background-color: #333;
  color: white;
  text-align: center;
  padding: 20px 0;
}

footer p {
  font-size: 1rem;
}
